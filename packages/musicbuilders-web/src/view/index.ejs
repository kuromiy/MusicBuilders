<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>ホーム</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
  </head>
  <body>
    <h1 class="title">ホーム</h1>
    <form action="/music-builders/logout" method="POST">
      <input type="submit" value="ログアウト">
    </form>
    <% if (model.hasTeamList()) { %>
      <% for (const team of model.teamViewDtoList) { %>
        <p><%= team.teamName %></p>
        <a href="/music-builders/team/<%= team.teamId %>/project/register">プロジェクト登録</a>
        <a href="/music-builders/team/<%= team.teamId %>/detail">プロジェクト詳細</a>
      <% } %>
    <% } else { %>
      <div>所属しているチームはありません。</div>
    <% } %>
  </body>
</html>